"use client";
import { useEffect, useRef, useState } from "react";
import { ChevronDown, LogOut } from "lucide-react";
import { useRouter } from "next/navigation";
import { useAuthStore } from "@/lib/auth-store";
import { useLanguage } from "@/components/language-provider";

export function ProfileMenu() { const router = useRouter(); const user = useAuthStore((state) => state.user); const logout = useAuthStore((state) => state.logout); const { t } = useLanguage(); const [open, setOpen] = useState(false); const ref = useRef<HTMLDivElement>(null); useEffect(() => { const close = (event: MouseEvent) => { if (ref.current && !ref.current.contains(event.target as Node)) setOpen(false); }; document.addEventListener("mousedown", close); return () => document.removeEventListener("mousedown", close); }, []); const handleLogout = () => { logout(); router.replace("/login"); }; return <div className="relative" ref={ref}><button aria-label={t("account")} aria-expanded={open} onClick={() => setOpen((value) => !value)} className="flex min-h-11 items-center gap-2 rounded-xl border border-[#ddd9cf] bg-white/70 px-2 text-left"><span className="grid size-8 place-items-center rounded-full bg-[#d8dbec] text-xs font-bold text-[#536080]">{user?.name?.slice(0, 2).toUpperCase() ?? "AW"}</span><span className="hidden max-w-24 truncate text-xs font-semibold text-slate-700 sm:block">{user?.name ?? t("account")}</span><ChevronDown size={15} className={`hidden text-slate-500 transition sm:block ${open ? "rotate-180" : ""}`} /></button>{open && <div className="profile-popover absolute right-1 top-[calc(100%+0.65rem)] z-50 w-64 rounded-2xl border border-slate-200 bg-white p-3 shadow-2xl"><div className="border-b border-slate-100 px-2 pb-3"><p className="text-sm font-semibold text-slate-900">{user?.name ?? t("account")}</p><p className="mt-1 text-xs text-slate-500">{user?.phone}</p></div><button onClick={handleLogout} className="mt-2 flex min-h-11 w-full items-center gap-2 rounded-xl px-2 text-sm font-semibold text-rose-600 hover:bg-rose-50"><LogOut size={16} />{t("logout")}</button></div>}</div>; }
